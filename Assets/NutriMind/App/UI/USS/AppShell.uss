/* App shell — shared chrome for NutriMind application screens (1920×1080 landscape) */

.app-shell {
    flex-grow: 1;
    width: 100%;
    height: 100%;
    flex-direction: column;
    background-color: var(--color-bg);
    padding: 0;
    margin: 0;
    position: relative;
    overflow: hidden;
}

.app-shell Label {
    margin: 0;
    padding: 0;
}

.app-shell__background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: var(--color-bg);
}

.app-shell__main {
    flex-grow: 1;
    flex-shrink: 1;
    width: 100%;
    height: 100%;
    flex-direction: column;
    min-height: 0;
}

/* ========== TOP BAR ========== */
.app-shell__top-bar {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    flex-shrink: 0;
    height: 96px;
    min-height: 96px;
    padding: 0 40px;
    background-color: var(--color-surface);
    border-bottom-width: 1px;
    border-bottom-color: var(--color-border);
}

.app-shell__top-bar-left {
    flex-direction: row;
    align-items: center;
    flex-grow: 1;
    flex-shrink: 1;
    min-width: 0;
    margin-right: 24px;
}

.app-shell__brand {
    flex-direction: row;
    align-items: flex-end;
    flex-shrink: 0;
    margin-right: 28px;
}

.app-shell__brand-nutri {
    font-size: 36px;
    color: var(--nm-brand-green);
    -unity-font-style: bold;
    letter-spacing: -1px;
}

.app-shell__brand-mind-wrap {
    position: relative;
    flex-direction: row;
    align-items: flex-end;
}

.app-shell__brand-mind {
    font-size: 36px;
    color: var(--nm-brand-blue);
    -unity-font-style: bold;
    letter-spacing: -1px;
}

.app-shell__brand-leaf {
    position: absolute;
    right: -4px;
    top: -4px;
    width: 18px;
    height: 18px;
    -unity-background-image-tint-color: var(--nm-brand-green);
}

.app-shell__page-copy {
    flex-direction: column;
    justify-content: center;
    flex-grow: 1;
    flex-shrink: 1;
    min-width: 0;
}

.app-shell__page-title {
    font-size: 28px;
    color: var(--color-text-primary);
    -unity-font-style: bold;
    letter-spacing: -0.5px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    margin-bottom: 2px;
}

.app-shell__page-context {
    font-size: 15px;
    color: var(--color-text-secondary);
    -unity-font-style: bold;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.app-shell__top-actions {
    flex-direction: row;
    align-items: center;
    flex-shrink: 0;
}

.app-shell__connection {
    flex-direction: row;
    align-items: center;
    flex-shrink: 0;
    height: 44px;
    padding: 0 14px;
    margin-right: 12px;
    border-radius: 14px;
    background-color: var(--color-surface-elev);
    border-width: 1px;
    border-color: var(--color-border);
}

.app-shell__connection-dot {
    width: 8px;
    height: 8px;
    border-radius: 4px;
    background-color: var(--color-primary);
    margin-right: 8px;
    flex-shrink: 0;
}

.app-shell__connection-icon {
    width: 16px;
    height: 16px;
    margin-right: 8px;
    flex-shrink: 0;
    -unity-background-image-tint-color: var(--color-text-secondary);
}

.app-shell__connection-label {
    font-size: 14px;
    color: var(--color-text-secondary);
    -unity-font-style: bold;
    white-space: nowrap;
}

.app-shell__connection--online .app-shell__connection-dot {
    background-color: var(--color-primary);
}

.app-shell__connection--offline .app-shell__connection-dot {
    background-color: var(--color-text-disabled);
}

.app-shell__connection--offline .app-shell__connection-icon {
    -unity-background-image-tint-color: var(--color-text-disabled);
}

.app-shell__connection--sync-pending .app-shell__connection-dot {
    background-color: var(--color-secondary);
}

.app-shell__connection--sync-pending .app-shell__connection-icon {
    -unity-background-image-tint-color: var(--color-secondary);
}

.app-shell__connection--sync-error .app-shell__connection-dot {
    background-color: var(--color-danger);
}

.app-shell__connection--sync-error .app-shell__connection-icon {
    -unity-background-image-tint-color: var(--color-danger);
}

.app-shell__connection--sync-error .app-shell__connection-label {
    color: var(--color-danger);
}

.app-shell__action-btn {
    width: 44px;
    height: 44px;
    min-width: 44px;
    min-height: 44px;
    margin-right: 12px;
    border-radius: 14px;
}

.app-shell__action-icon {
    width: 20px;
    height: 20px;
}

.app-shell__notifications {
    position: relative;
    overflow: visible;
}

.app-shell__notifications-badge {
    position: absolute;
    top: -4px;
    right: -4px;
    min-width: 22px;
    min-height: 22px;
    padding-left: 5px;
    padding-right: 5px;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background-color: var(--color-danger);
}

.app-shell__notifications-badge.app-shell__notifications-badge--hidden {
    display: none;
}

.app-shell__notifications-badge-label {
    margin: 0;
    padding: 0;
    font-size: 13px;
    -unity-font-style: bold;
    color: var(--color-text-on-accent);
    -unity-text-align: middle-center;
    white-space: nowrap;
}

.app-shell__profile {
    flex-direction: row;
    align-items: center;
    height: 56px;
    min-height: 56px;
    padding: 4px 14px 4px 4px;
    margin: 0;
    border-radius: 18px;
    background-color: var(--color-surface-elev);
    border-width: 1px;
    border-color: var(--color-border);
}

.app-shell__profile:hover {
    background-color: var(--color-surface-hover);
}

.app-shell__profile > .unity-button__text {
    display: none;
    width: 0;
    height: 0;
    margin: 0;
    padding: 0;
    font-size: 0;
    color: rgba(0, 0, 0, 0);
}

.app-shell__avatar {
    width: 48px;
    height: 48px;
    min-width: 48px;
    min-height: 48px;
    border-radius: 24px;
    background-color: #E8F5E9;
    border-width: 2px;
    border-color: #C8E6C9;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    margin-right: 12px;
    flex-shrink: 0;
}

.app-shell__avatar-label {
    font-size: 9px;
    color: #7CB342;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    white-space: normal;
}

.app-shell__profile-copy {
    flex-direction: column;
    justify-content: center;
    margin-right: 10px;
    flex-shrink: 1;
    min-width: 0;
}

.app-shell__profile-name {
    font-size: 16px;
    color: var(--color-text-primary);
    -unity-font-style: bold;
    white-space: nowrap;
}

.app-shell__profile-meta {
    font-size: 13px;
    color: var(--color-text-secondary);
    -unity-font-style: bold;
    white-space: nowrap;
}

.app-shell__profile-chevron {
    width: 18px;
    height: 18px;
    flex-shrink: 0;
    -unity-background-image-tint-color: var(--color-text-secondary);
}

/* ========== OFFLINE / SYNC BANNER HOST ========== */
/* Layout host only — OfflineSyncBanner owns visuals, tones, and copy. */
.app-shell__offline-banner {
    width: 100%;
    flex-shrink: 0;
}

/* ========== CONTENT REGION ========== */
.app-shell__content-region {
    flex-grow: 1;
    flex-shrink: 1;
    min-width: 0;
    min-height: 0;
    padding: 18px 28px 14px 28px;
    position: relative;
    overflow: hidden;
    align-items: stretch;
    justify-content: flex-start;
}

.app-shell__content-instance {
    flex-grow: 1;
    flex-shrink: 1;
    flex-direction: column;
    width: 100%;
    height: 100%;
    min-width: 0;
    min-height: 0;
    align-self: stretch;
    overflow: hidden;
}

/* Instantiated panel roots must fill the TemplateContainer host. */
.app-shell__content-instance > .app-screen-content {
    flex-grow: 1;
    flex-shrink: 1;
    width: 100%;
    height: 100%;
    min-width: 0;
    min-height: 0;
}

/* ========== BOTTOM NAV ========== */
.app-shell__navigation-region {
    flex-direction: row;
    align-items: center;
    justify-content: space-around;
    flex-shrink: 0;
    height: 104px;
    min-height: 104px;
    padding: 10px 40px 14px 40px;
    background-color: var(--color-surface);
    border-top-width: 1px;
    border-top-color: var(--color-border);
}

.app-shell__nav-item {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    flex-grow: 1;
    flex-basis: 0;
    height: 80px;
    margin: 0 6px;
    padding: 10px 8px;
    background-color: rgba(0, 0, 0, 0);
    border-width: 0;
    border-radius: 18px;
}

.app-shell__nav-item:hover {
    background-color: var(--color-surface-hover);
}

.app-shell__nav-item.is-active {
    background-color: var(--color-primary-soft);
    border-width: 1px;
    border-color: rgba(76, 175, 80, 0.35);
}

.app-shell__nav-item.is-active .app-shell__nav-icon {
    -unity-background-image-tint-color: var(--color-primary);
}

.app-shell__nav-item.is-active .app-shell__nav-label {
    color: var(--color-primary);
}

.app-shell__nav-item > .unity-button__text {
    display: none;
    width: 0;
    height: 0;
    margin: 0;
    padding: 0;
    font-size: 0;
    color: rgba(0, 0, 0, 0);
}

.app-shell__nav-icon {
    width: 32px;
    height: 32px;
    margin-bottom: 8px;
    -unity-background-image-tint-color: #757575;
}

.app-shell__nav-label {
    font-size: 14px;
    color: #5C5C5C;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    white-space: nowrap;
}

/* ========== OVERLAY LAYERS ========== */
.app-shell__overlay-layer {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
}

.app-shell__toast-layer {
    position: absolute;
    right: var(--space-4);
    bottom: 120px;
    width: 420px;
    max-width: 42%;
    flex-direction: column;
    align-items: stretch;
    justify-content: flex-end;
}

.app-shell__toast {
    width: 100%;
}

.app-shell__toast--hidden {
    display: none;
}

.app-shell__modal-layer {
    align-items: center;
    justify-content: center;
}

.app-shell__modal-layer--empty {
    /* Host only — no dim fill while empty so chrome stays interactive. */
    background-color: rgba(0, 0, 0, 0);
}

.app-shell__modal-placeholder--hidden {
    display: none;
}

/* ========== MORE HUB (secondary destinations) ========== */
.app-shell__more-hub {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
    padding: 28px 36px;
}

.app-shell__more-hub-backdrop {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(28, 24, 20, 0.46);
}

.app-shell__more-hub-card {
    position: relative;
    flex-direction: column;
    align-items: stretch;
    flex-grow: 0;
    flex-shrink: 1;
    /* Scale with device width: ~46% of shell, clamped for tablets. */
    width: 46%;
    min-width: 440px;
    max-width: 680px;
    max-height: 92%;
    padding: 36px 36px 28px 36px;
    background-color: var(--color-surface);
    border-radius: 28px;
    border-width: 2px;
    border-color: var(--color-border-strong);
}

.app-shell__more-hub-header {
    flex-direction: row;
    align-items: center;
    width: 100%;
    margin-bottom: 22px;
}

.app-shell__more-hub-header-icon-bg {
    width: 64px;
    height: 64px;
    margin-right: 16px;
    border-radius: 32px;
    background-color: var(--color-primary-soft);
    border-width: 2px;
    border-color: rgba(76, 175, 80, 0.28);
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.app-shell__more-hub-header-icon {
    width: 32px;
    height: 32px;
    -unity-background-image-tint-color: var(--color-primary);
}

.app-shell__more-hub-header-copy {
    flex-grow: 1;
    flex-shrink: 1;
    flex-direction: column;
    min-width: 0;
}

.app-shell__more-hub-title {
    font-size: 32px;
    color: var(--color-text-primary);
    -unity-font-style: bold;
    margin-bottom: 4px;
}

.app-shell__more-hub-subtitle {
    font-size: 18px;
    color: var(--color-text-secondary);
    -unity-font-style: bold;
    white-space: normal;
}

.app-shell__more-hub-list {
    flex-direction: column;
    width: 100%;
    margin-bottom: 18px;
}

.app-shell__more-hub-item {
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    width: 100%;
    min-height: 72px;
    margin-bottom: 12px;
    padding: 14px 18px;
    background-color: var(--color-bg);
    border-width: 2px;
    border-color: var(--color-border);
    border-radius: 18px;
    color: var(--color-text-primary);
    -unity-font-style: bold;
}

.app-shell__more-hub-item:hover {
    border-color: var(--color-primary);
    background-color: var(--color-primary-soft);
}

.app-shell__more-hub-item-icon-bg {
    width: 48px;
    height: 48px;
    margin-right: 14px;
    border-radius: 14px;
    background-color: var(--color-surface);
    border-width: 1px;
    border-color: var(--color-border);
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.app-shell__more-hub-item-icon {
    width: 24px;
    height: 24px;
    -unity-background-image-tint-color: var(--color-primary);
}

.app-shell__more-hub-item-label {
    flex-grow: 1;
    flex-shrink: 1;
    min-width: 0;
    font-size: 20px;
    -unity-font-style: bold;
    -unity-text-align: middle-left;
    color: var(--color-text-primary);
}

.app-shell__more-hub-item-chevron {
    width: 22px;
    height: 22px;
    flex-shrink: 0;
    -unity-background-image-tint-color: var(--color-text-secondary);
}

.app-shell__more-hub-close {
    margin-top: 4px;
    min-height: 56px;
    height: 56px;
    justify-content: center;
    border-radius: 18px;
    font-size: 18px;
}

.app-shell--compact .app-shell__more-hub {
    padding: 20px 24px;
}

.app-shell--compact .app-shell__more-hub-card {
    width: 58%;
    min-width: 360px;
    max-width: 560px;
    padding: 28px 26px 22px 26px;
}

.app-shell--compact .app-shell__more-hub-title {
    font-size: 28px;
}

.app-shell--compact .app-shell__more-hub-subtitle {
    font-size: 16px;
}

.app-shell--compact .app-shell__more-hub-item {
    min-height: 64px;
    margin-bottom: 10px;
}

.app-shell--compact .app-shell__more-hub-item-label {
    font-size: 18px;
}

.app-shell--narrow .app-shell__more-hub-card {
    width: 92%;
    min-width: 0;
    max-width: 520px;
    padding: 24px 20px 18px 20px;
}

.app-shell--narrow .app-shell__more-hub-header-icon-bg {
    width: 52px;
    height: 52px;
    border-radius: 26px;
}

.app-shell--narrow .app-shell__more-hub-item {
    min-height: 60px;
    padding: 12px 14px;
}

.app-shell__loading-layer {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
}

/* ========== RESPONSIVE: compact < 1100 ========== */
.app-shell--compact .app-shell__top-bar {
    height: auto;
    min-height: 80px;
    padding: 12px 24px;
    flex-wrap: wrap;
}

.app-shell--compact .app-shell__brand-nutri,
.app-shell--compact .app-shell__brand-mind {
    font-size: 28px;
}

.app-shell--compact .app-shell__page-title {
    font-size: 22px;
}

.app-shell--compact .app-shell__page-context {
    font-size: 13px;
}

.app-shell--compact .app-shell__connection-label {
    font-size: 13px;
}

.app-shell--compact .app-shell__profile-copy {
    display: none;
}

.app-shell--compact .app-shell__profile {
    padding: 4px;
    width: 56px;
    min-width: 56px;
    justify-content: center;
}

.app-shell--compact .app-shell__profile-chevron {
    display: none;
}

.app-shell--compact .app-shell__avatar {
    margin-right: 0;
}

.app-shell--compact .app-shell__content-region {
    padding: 20px 24px 14px 24px;
}

.app-shell--compact .app-shell__navigation-region {
    height: auto;
    min-height: 80px;
    padding: 8px 12px 12px 12px;
    flex-wrap: wrap;
}

.app-shell--compact .app-shell__nav-item {
    flex-grow: 0;
    width: 84px;
    height: 72px;
}

.app-shell--compact .app-shell__toast-layer {
    right: var(--space-3);
    width: 380px;
    max-width: 55%;
    bottom: 100px;
}

/* ========== RESPONSIVE: narrow < 820 ========== */
.app-shell--narrow .app-shell__brand {
    margin-right: 16px;
}

.app-shell--narrow .app-shell__brand-nutri,
.app-shell--narrow .app-shell__brand-mind {
    font-size: 24px;
}

.app-shell--narrow .app-shell__page-title {
    font-size: 18px;
    white-space: normal;
}

.app-shell--narrow .app-shell__page-context {
    display: none;
}

.app-shell--narrow .app-shell__connection {
    padding: 0 10px;
    margin-right: 8px;
}

.app-shell--narrow .app-shell__connection-label {
    display: none;
}

.app-shell--narrow .app-shell__action-btn {
    margin-right: 8px;
}

.app-shell--narrow .app-shell__nav-label {
    font-size: 11px;
}

.app-shell--narrow .app-shell__nav-icon {
    width: 28px;
    height: 28px;
    margin-bottom: 4px;
}

.app-shell--narrow .app-shell__toast-layer {
    left: var(--space-3);
    right: var(--space-3);
    width: auto;
    max-width: none;
}
